/* Decorative deterministic chart only; never reads or alters game/wallet state. */
(function(){'use strict';var canvas=document.getElementById('terminalChart');if(!canvas)return;function draw(){var r=canvas.getBoundingClientRect();if(!r.width)return;var d=Math.min(window.devicePixelRatio||1,2);canvas.width=Math.round(r.width*d);canvas.height=Math.round(r.height*d);var c=canvas.getContext('2d');if(!c)return;c.scale(d,d);var w=r.width,h=r.height;c.strokeStyle='#274238';c.lineWidth=.5;for(var y=15;y<h;y+=35){c.beginPath();c.moveTo(0,y);c.lineTo(w,y);c.stroke()}for(var x=0;x<w;x+=55){c.beginPath();c.moveTo(x,0);c.lineTo(x,h);c.stroke()}var last=65;for(var i=0;i<60;i++){var close=65+i*.6+Math.sin(i*.43)*7+Math.sin(i*1.7)*3;var px=(i+.5)*w/60,openY=h*.82-(last-55)*h/65,closeY=h*.82-(close-55)*h/65;var color=close>=last?'#fa7883':'#5aaeff';c.strokeStyle=c.fillStyle=color;c.beginPath();c.moveTo(px,Math.min(openY,closeY)-5);c.lineTo(px,Math.max(openY,closeY)+5);c.stroke();c.fillRect(px-w/240,Math.min(openY,closeY),Math.max(2,w/120),Math.max(2,Math.abs(closeY-openY)));c.globalAlpha=.18;c.fillRect(px-w/240,h-6-Math.abs(close-last)*3,Math.max(2,w/120),6+Math.abs(close-last)*3);c.globalAlpha=1;last=close;}}if(window.ResizeObserver)new ResizeObserver(draw).observe(canvas);else window.addEventListener('resize',draw);draw();})();
