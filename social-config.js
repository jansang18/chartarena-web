/* Public configuration only. Never put provider secrets in this file.
   Enable after completing docs/social-login-setup.md and real-account verification. */
window.ArenaSocialConfig = Object.freeze({endpoint:'',googleEnabled:false});
