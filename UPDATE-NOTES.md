## 2026-09-20 — 5라운드와 GO / STOP

- 한 경기 5라운드. 매 라운드 진입(방향·배율) → 30봉 GO/STOP → 60봉 GO/STOP → 90봉 자동 정산. 최대 15번 결정하며 STOP 이후에는 해당 라운드를 관전합니다.
- 중간 결정은 10초, 시간 초과는 STOP. 처음 진입 가격과 레버리지를 유지하고 STOP 시점의 누적 수익률로 한 번만 정산합니다. 확정한 진입/GO는 다음 30봉 구간까지 진행한 결과를 중도 종료 시에도 정산합니다.
- 실제 Binance 연속 300봉 차트 5,000개(20종목 × 5시간봉 × 50구간). 과거 210봉, 미래 90봉. 동일 종목·시간봉 내 구간은 겹치지 않습니다. 각 종목의 다른 시간봉은 같은 실제 시장 시기를 포함할 수 있습니다. 원본 요청과 체크섬은 docs/battle-chart-sources.json에 기록했습니다.
- 최근 500개 차트 재등장 방지(해당 기기 기록 기준). 공개된 과거 움직임만으로 변동 구간을 분류합니다. 차트 데이터는 라운드마다 필요한 약 13KB만 지연 로딩합니다. 기존 charts.js는 다른 소비자를 위해 보존했습니다.
- 모바일 GO/STOP은 진입 컨트롤을 대체하므로 새 하단 배너가 생기지 않습니다. 온라인 방 규칙은 v6으로 분리하고 단계·STOP 시점·결정 이력을 동기화합니다.
- 로컬 브라우저에서 5라운드 종료, 30봉 GO → 60봉 STOP 및 고정 손익을 확인했습니다. 온라인 프로토콜은 자동 테스트로 검증했으며 실제 두 계정 Firebase 경기는 이번 검증 범위에 포함되지 않았습니다.

## 2026-09-19 — 1%당 골드로 정산하는 4인 배틀

- 손익 = 방향을 반영한 차트 수익률(소수점 4자리) × 방의 1%당 골드 × 레버리지. 골드는 정수 반올림. 일반방에서 +1%/10배 = +10,000골드, 반대 방향은 -10,000골드.
- 연습방(모의 100), 입문방(100), 일반방(1,000), 고수방(10,000) 선택. 골드방은 단가의 20배를 경기 자금으로 확보하며, 참가비가 아닌 반환 가능한 자금입니다. 경기 순손실은 처음 확보한 금액을 넘지 않습니다.
- 투입 비율, 소극적/보통/공격적, 승부수/방어, 순위별 추가 골드 보상을 제거. 방향과 레버리지만 선택. 기본 1배이며 2/3/5/10배를 다시 누르면 1배. 패스 1회와 미제출은 0골드; 경기 자금 소진 후 관전.
- 확정된 선택과 정산 상태를 기존 지갑 문서에 함께 저장. 중도 종료/새로고침 때 확정된 라운드의 최종 결과까지 정산하고 잔액 반환. 중복 정산 차단, 최근 정산 내역 표시. 연습방은 보유 골드/경험치 변동 없음.
- 온라인 방은 rulesVersion 3 및 같은 tableId만 매칭. 기존 실사용자 매칭의 Firebase 권한 오류는 이번 변경으로 해결하지 않았으며 봇 포함 모드는 명시됩니다. 골드는 기존과 동일한 기기 로컬 가상 재화 저장 방식입니다.
- 폰에서 전략 서랍을 제거하고 방향/레버리지/확정/계산식을 첫 화면에 배치. 금액 축약을 없애고 결과 팝업의 좁은 화면 줄바꿈 보정.

# 화면 구조 개선 — 2026-09-19

## 4인 배틀 중심 홈

- 홈과 배틀의 네이비·블루·골드 색상을 통일했습니다. 파스텔 카드별 색 구분을 제거하고 메인 배틀 배너와 입장 버튼에 시각적 강조를 집중했습니다.
- 기존 네 명의 캐릭터 이미지를 큰 메인 배너에 사용하고, 배틀을 홈의 첫 번째 행동으로 배치했습니다. 연습·퍼즐·일일 도전은 보조 영역, 연습 기록은 하단으로 이동했습니다.
- 반복되는 상점/내 기록 카드를 제거하고 계정 메뉴를 유지했습니다. 전체 탐색에서도 배틀을 연습보다 먼저 표시합니다. 휴대폰 홈의 중복 상단 탐색은 콘텐츠 진입 링크로 통합했습니다.
- 좁은 화면과 폴드 가로 화면에서 배틀 진입이 늦어지지 않도록 프로필/배너 높이를 조정했습니다. 기존 캐릭터·소유 아이템·저장 기록·게임 규칙은 유지합니다.

## 휴대폰 경기 화면 집중 모드

- 레버리지는 롱·숏·패스 오른쪽에 항상 표시합니다. 기본 1배에서 시작하며 2·3·5·10배 네 버튼만 표시하고, 선택된 배율을 다시 누르면 1배로 돌아갑니다. 확정 버튼에도 현재 배율을 표시합니다. 투입/전술은 기존 펼침 패널에 유지합니다.

- 경기 중 공통 로고·전체 탐색 메뉴를 숨기고 종료하면 복원합니다. 라운드·경기 자금·종료만 48px 상태줄에 남깁니다.
- 휴대폰/폴드/높이가 작은 화면에서 차트 다음에 방향 선택과 확정을 배치하고 전략·전술·고급 설정은 펼침 패널로 모았습니다. 선택한 전략은 접힌 제목에도 표시합니다.
- 참가자는 4열의 작은 상반신·점수 패널로 축소했습니다. 넓고 높이가 작은 화면에서는 차트 왼쪽, 조작 오른쪽의 두 열로 배치합니다.
- 320×480, 344×580, 390×650, 690×740, 829×565, 854×390, 1280×650, 1920×1080에서 방향/확정 버튼이 첫 화면 안에 들어오는지 측정했습니다. 새 주 조작 버튼 높이는 48px이며 가로 넘침이 없습니다.
- 세로 화면에서 전략 펼침 → 롱/방어 선택 → 접기 → 확정 → 결과 공개와 종료 후 메뉴 복원을 브라우저로 확인했습니다. 실제 휴대폰 장치 및 카카오 브라우저 자체의 주소창은 제어하거나 검증하지 않았습니다.

## 게임성 확장

- 4인 배틀은 5라운드, 모두 경기 전용 10,000P로 시작합니다. 경기 손익은 상점 골드에 영향을 주지 않고 완주 순위에 따라 골드 보상을 한 번 지급합니다.
- 소극적·보통·공격적 선택, 고급 투입/배율 설정, 경기당 무료 패스 1회와 승부수/방어 중 전술 1회를 추가했습니다. 미제출은 -200P이며 패스를 소모하지 않습니다.
- 참가자 선택 동시 공개, 순차 캔들 공개 중 예상 순위, 승패 반응, 역전 하이라이트, 우승 연출을 추가했습니다. 온라인에서는 라운드 기록으로 지연된 참가자의 점수를 복구합니다.
- 기존 30레벨 진행을 유지하면서 방향, 지지·저항 구간, 손절 거리, 돌파 유지/실패 문제와 혼합 보스를 제공합니다. 유형별 자유 연습은 진행 기록과 재화를 바꾸지 않습니다.
- 한국 날짜 기준 같은 차트 3개의 일일 도전을 추가했습니다. 첫 도전만 기록에 반영하고 재연습은 별도입니다. 다른 날 1·3·7회 완료하면 프레임과 연출 자세가 해금됩니다.
- 운영 온라인 기록은 기존 Firebase 익명 계정과 scores 문서를 사용합니다. 로컬/오프라인은 기기 기록임을 표시합니다. 서버 권한 규칙은 변경하지 않았습니다.
- 브라우저에서 5라운드 완주와 보상, 다음 경기의 자금/전술 초기화, 퍼즐 네 유형, 일일 도전 완료·재연습·새로고침을 확인했습니다. 홈·일일 도전·경기 조작부는 344/690/1920px에서 가로 넘침 없이 표시되고 새 조작 버튼은 높이 44px 이상입니다.
- 실제 갤럭시 폴드 하드웨어 및 실제 사람 네 명의 동시 대전은 별도 검증이 필요합니다. 서버에서 점수를 검증하는 부정행위 방지 기능은 이번 범위에 포함하지 않습니다.
- 운영 연결 확인: 일일 기록 조회는 HTTP 200입니다. 대전방 rooms 조회는 익명 로그인 후에도 HTTP 403 PERMISSION_DENIED로 거부되어 실제 사람 매칭은 봇 대전으로 전환됩니다. 운영 Firebase 소유자가 대전방의 최소 권한 규칙을 구성한 뒤 온라인 동시 대전을 추가 검증해야 합니다. 이 제한은 프런트엔드 코드만으로 해결할 수 없습니다.
- 자동 검증: 규칙·동기화·취소 경쟁 상태·첫 기록 보존·자산 해시 등 53개 테스트 통과, 인라인 JS/자산 경로/홈 사본 검사 통과.

현재 웹 게시본과 실행 가능한 전체 ZIP을 제공하며, 아래의 배포 전 문구와 이전 테스트 수는 최초 수정 당시 기록입니다.

## 밝은 홈, 확대된 배틀 캐릭터, 보석교환 수정

- 홈은 화이트·블루·골드, 배틀은 채도 있는 네이비와 참가자별 컬러로 변경. 기존 캐릭터/로고 이미지를 그대로 사용.
- 배틀 캐릭터 이미지 높이 96px → 290px, 174px 높이의 상반신 영역에 표시. 휴대폰은 184px 이미지/110px 영역, 참가자 2×2 배치. 이름·점수 확대.
- 보석교환이 상품 한 칸에 갇히던 문제 수정: 전체 열 사용, 상점 레이아웃을 실제 Grid로 적용, 좁은 폭에서는 버튼을 다음 줄로 배치. 카테고리와 교환 액션은 네이티브 버튼으로 변경.
- 홈/경기 8개 폭, 상점 3개 분류×5개 폭, 연습/퍼즐/학습 3개 폭: 총 40개 조합에서 가로 넘침 없음. 보석교환 버튼 44px 이상 및 카드 내부 배치 확인.
- 로컬 봇 매칭과 확대된 경기 화면, 로컬 보석 1개 → 50,000 골드 교환 확인. 실제 폴드 하드웨어 및 실제 4명 동시 대전은 미검증.

## 추가 수정: 캐시 오류와 폴더블 대응

- 기존 방문자의 오래된 `energy.js` 캐시 때문에 `NRG.createPass is not a function`이 발생해 매칭/연습 버튼 초기화가 멈추는 문제 수정. 공통 파일 URL에 콘텐츠 해시를 붙이고 검사 추가.
- 배틀의 중복 모드 선택 카드를 하나의 로비로 통합. 실제 HTML 버튼으로 매칭 시작. 큰 화면에서는 이미지/설명을 나란히, 중간 폭에서는 이미지 아래 두 열, 좁은 화면에서는 세로 배치.
- 공통 최대 폭 1800px. 홈 상단과 여백을 압축하고 중간 폭의 프로필을 가로 배치. 680~1000px 연습 화면은 차트/조작부 2열, 휴대폰은 1열.
- 320, 344, 360, 390, 600, 690, 768, 829, 844, 1024, 1440, 1920, 2560px 폭과 세로/가로 높이 조합에서 주요 5페이지 총 65개 검증: 가로 넘침 없음, 매칭 버튼 최소 44px 이상. 브라우저 오류 없음.
- 오류가 나던 공개 사이트 브라우저에서 새로고침 후 매칭 → 봇 포함 4인 1라운드 진입 확인. 로컬에서는 매칭/게임 중 344↔690px 변경 시 게임 상태와 차트 표시 유지 확인.
- 실제 갤럭시 폴드 기기를 연결한 검증은 아니며, 브라우저에서 화면 크기를 바꿔 검증함. 실제 사람 4명 동시 대전은 미검증.
- 검증 명령: `node scripts/version-assets.cjs`, `node --test tests/*.test.cjs` (8개), `node tests/verify-build.cjs`.

아래는 최초 수정 당시 기록입니다.

- 홈·차트 연습·4인 배틀·퍼즐·학습에 공통 탐색과 최대 폭 1440px 적용. 고정 크기 화면을 통째로 축소하던 로직 제거.
- 모바일에서는 세로 재배치. 차트 연습의 차트 높이 300px, 핵심 조작 최소 높이 44px. 모바일 배틀은 차트 → 조작 → 참가자 순서.
- 1대1 로비, 진입, 상태와 정산 코드 제거. 4인 온라인 매칭과 봇 대체 흐름 유지. 봇 참가자임을 명시.
- 홈의 고정 순위·접속자 수·롱숏 비율·시즌 타이머 제거. 차트 연습 기록으로 대체. 준비 중인 훈련·설정·알림 진입 제거.
- 랭킹에 섞던 고정 봇 제거. 서버 기록과 기기 기록 상태 표시. 기존 서버 테스트 기록은 삭제하지 않음.
- 연습은 예측 확정 시, 배틀은 매칭 완료 후 경기 시작 시 플레이 1회 사용. 같은 연습 문제의 다시 풀기는 무료.
- 기존 캐릭터·로고·차트 데이터·저장 키 유지. 로컬 미리보기에서는 Firebase 초기화를 건너뛰어 운영 기록에 쓰지 않음.

## 실행

ZIP 전체를 압축 해제한 뒤 `index.html`을 브라우저로 엽니다. HTML 파일만 따로 옮기지 마세요. 모든 assets와 JS/CSS 파일이 필요합니다.

localhost 또는 file URL에서는 로컬 기록과 봇 배틀을 사용합니다. 운영 호스트에서는 기존 Firebase 연결을 사용합니다. 이 수정본은 아직 GitHub Pages에 배포하지 않았습니다.

## 검증

- `node --test tests/*.test.cjs`: 7개 통과. 횟수 중복 차감, 잔여 0회 재도전, 다른 탭의 소비, 배틀 순차 공개·정산·손실 상한·타임아웃 검증.
- `node tests/verify-build.cjs`: 인라인 JS 구문, 자산 경로, 공통 UI 연결, 홈 원본/사본 일치, 1대1 제거 확인.
- 브라우저: 320×700, 390×844, 768×1024, 844×390, 1024×768, 1440×900에서 주요 5페이지 총 30개 조합. 문서 가로 넘침 없음.
- 최초 캐릭터/닉네임 설정, 보상 팝업, 기록·상점·길드 진입, 연습 제출/결과/다시 풀기/새로고침, 퍼즐 선택/결과/다음 문제, 배틀 매칭 취소/봇 시작/확정/순차 공개/정산/다음 라운드 확인.
- 실제 사람 4명의 온라인 동시 접속, Android WebView, 실제 휴대전화의 키보드/터치 환경은 미검증.

## 유지보수

공통 UI는 `app-shell.css`, `app-shell.js`에서 관리합니다. 홈 변경 후 `landscape.html`을 `index.html`에 복사합니다. 기존 AGENTS.md의 고정 프레임/1대1/라이트 테마 설명은 이 변경으로 대체됩니다.
